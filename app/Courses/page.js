import React from "react";
// import Header from "@/Components/Header";
const courses=()=>{
    return(
        
        <div>
            {/* since we have included the header in the layout.js so there is no need to add header to every page differently the layout,js will automatically include the header on every page */}
            {/* <Header/> */}
            courses</div>
    )
}

export default courses